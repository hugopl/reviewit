<h1><%= @project.name %></h1>

<p><%= link_to 'Merge requests', project_merge_requests_path(@project) %></p>

<p>To setup rme on your project working copy, do:</p>
<code>$ curl -f <%= full_setup_url %> | ruby</code></p>

