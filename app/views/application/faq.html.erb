<h1>Frequent Asked Questions</h1>

<h2>How do I create a merge request?</h2>
<p>The only way to create a merge request is using the command line interface (CLI), after get it installed and configured into your project type:</p>

<p><code>review push TARGET_BRANCH</code></p>

<p>This will create a merge request with the last commit on the current branch (git HEAD), after accepted the patch will be merged into the branch <code>TARGET_BRANCH</code>.</p>

<h2>How do I install the CLI?</h2>
<p>Go to the <%= link_to 'initial page', projects_path %>, click on the desired project. This will send you to the merge request list, now click on the project name in the bar on top of the page and follow the instructions.</p>

<h2>How do I update a merge request?</h2>
<p>Ammend your changes to your commit (<code>git commit --amend</code>) then type <code>review push</code>.</p>

<h2>How do I change the merge request target branch?</h2>
<p>With the commit related to the merge request on git HEAD type: <code>review push NEW_TARGET_BRANCH</code></p>
